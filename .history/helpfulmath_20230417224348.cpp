#include <bits/stdc++.h>
#include <string>
#include<cmath>
using namespace std;

int main()
{
    string s;
    cin>>s;
   vector<char>nums;
   for(auto i:s){
    if(i!='+')
    nums.push_back(i);
   }
}